import {ChevronLeft,ChevronRight,Expand,X,Warehouse} from 'lucide-react';
import {content} from '../data/mockData';
import {useGallery} from '../hooks/useGallery';
interface PhotoGalleryProps {readonly images:readonly string[];readonly title:string;}
export function PhotoGallery({images,title}:PhotoGalleryProps){const gallery=useGallery(images.length);const alt=`${title} · ${content.gallery.photo} ${gallery.index+1}`;if(!images.length)return <div className="no-photo"><Warehouse size={54} strokeWidth={1}/><h2>{content.gallery.specs}</h2><p>{content.gallery.noPhotos}</p></div>;return <section className="photo-gallery" aria-label={content.gallery.title}><div className="gallery-stage"><button className="gallery-expand" aria-label={content.gallery.enlarge} onClick={gallery.open}><img src={images[gallery.index]} alt={alt}/><span className="expand-badge"><Expand size={17}/></span></button><button className="gallery-arrow left" onClick={gallery.previous} aria-label={content.gallery.previous}><ChevronLeft/></button><button className="gallery-arrow right" onClick={gallery.next} aria-label={content.gallery.next}><ChevronRight/></button><span className="gallery-counter" role="status">{gallery.index+1} {content.gallery.of} {images.length}</span></div><div className="gallery-thumbnails">{images.map((image,index)=><button key={image} onClick={gallery.select.bind(null,index)} aria-label={`${content.gallery.photo} ${index+1}`} aria-pressed={gallery.index===index}><img src={image} alt="" loading="lazy" width="120" height="90"/></button>)}</div><dialog ref={gallery.dialog} className="gallery-dialog" onCancel={gallery.close} onClose={gallery.close} onKeyDown={gallery.keyDown} aria-label={content.gallery.title}><button className="gallery-close" onClick={gallery.close} aria-label={content.gallery.close}><X/></button><img src={images[gallery.index]} alt={alt}/><div className="lightbox-controls"><button className="gallery-arrow" onClick={gallery.previous} aria-label={content.gallery.previous}><ChevronLeft/></button><span>{gallery.index+1} {content.gallery.of} {images.length}</span><button className="gallery-arrow" onClick={gallery.next} aria-label={content.gallery.next}><ChevronRight/></button></div></dialog></section>;}
